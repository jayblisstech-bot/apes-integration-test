{
  "name": "apes-integration-test",
  "version": "1.0.0",
  "private": true,
  "description": "Controlled APES v1.2.0 integration harness",
  "scripts": {
    "typecheck": "node --check src/account-access.js",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "lint": "node --check src/account-access.js"
  },
  "engines": {
    "node": ">=20"
  }
}
